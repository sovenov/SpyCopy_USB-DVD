# 😈 SpyCopy_USB-DVD

> О, флешка! Дай-ка я всё твоё добро спи... копирую.

**Плохая программа для скрытого сканирования и копирования внешних носителей.**

---

## 🛠 Возможности и Совместимость

* **Кроссплатформенность:** Работает на **Windows, Linux, macOS** — хоть на утюге, если Python прикрутишь.
* **Сборка для Windows:** Для Windows можно собрать `.exe` и забыть, что Python вообще существует.
* **Автоопределение носителя:** Скрипт сам определяет, что вы воткнули — **флешку, SD-шку, внешний диск, или вдруг вспомнили про CD/DVD** (да, они ещё бывают).

### 🚨 Как это работает (Спойлер: Грубо)

Как только носитель появляется — скрипт такой:

> «Поздравляю, родной. Сейчас мы посмотрим, что у тебя там внутри»

...и создаёт красивую папочку в `/catalogs`, со временем, датой, таймстампом и именем твоей флешки. Чтобы, не дай бог, не перепутать все эти 69 флешек, которые ты нашёл в шкафу.

---

## 🏗 Сборка в .exe

Используйте **PyInstaller** для создания автономного исполняемого файла.

1.  **Установить PyInstaller**
    ```bash
    pip install pyinstaller
    ```

2.  **Собрать проект**
    ```bash
    pyinstaller --noconsole --onefile main.py
    ```

После сборки исполняемый файл будет находиться здесь: `dist/main.exe`

---

## 🧐 Что скрипт умеет

Вообще он умный. **Реально.** Вот список:

* **Видит всё:** Обнаруживает любые носители, которые появляются: **USB, SD, HDD, SSD, CD/DVD**, ну и эти локальные диски, которые сами собой позже монтируются.
* **Гибкие режимы копирования:**
    * Ничего не копировать
    * Копировать только структуру
    * Копировать всё со структурой
    * Устроить бардак и скопировать всё в одну кучу
* **Генерация структуры:** Генерирует **JSON-структуру** папок для анализа.
* **Обработка дубликатов:** Если имена файлов одинаковые — добавляет **микросекундный timestamp**, чтобы не было `«фото111 (1).jpg»`.
* **Фильтрация:** Может копировать только нужные расширения и ограничивать максимальный размер файлов.
* **Стабильность:** Ведёт себя нормально даже на **Windows 7**, которой сто лет в обед.
* **Чистота:** Игнорирует системные каталоги, чтобы не копировать мусор.
* **Надёжность:** Спокойно переживает обрывы носителя.

---

### Если тупой и лень разбираться
Из каталога "Готовые exe", или из релизов, скачиваешь готовые exe файлы, запускаешь, пляшешь.

Exeшник появится в процессах диспетчера задач. Никакое окно не запустится. Завершать скрипт тоже через диспетчер.
Можно прокинуть в автозапуск windows
