# 😈 SpyCopy_USB-DVD

**Плохая программа для скрытого сканирования и копирования внешних носителей.**

---

## 🚨 Как это работает

Запускаешь скрипт, ну и всё вроде!

В фоновом режиме начинает мониторить флешки, dvd и тому прочее. Если что-то из-этого обнаруживает - тайно копирует информацию из носителя и кладёт её радям с собой, в папку "catalogs", которую сам же создаст если её нет.

Если флешка определяется как SSD, то есть как локальный диск - будет реагировать только при "втыкании" и "вытыкании" её.

---

## 🛠 Кроссплатформенность

- Работает на **Windows, Linux, macOS**, возможно заработает и на **Android, iOS, Symbian, BadaOS, BlackBerry OS**. Короче скрипт написан на питоне. Вот где на питоне его можно собрать в бинарник - там и заработает.

---

## 🏗 Сборка в `.exe`

Используй **PyInstaller** для создания автономного исполняемого файла.

1. **Установить PyInstaller**
   ```bash
   pip install pyinstaller
   ```

2. **Собрать проект**
   ```bash
   pyinstaller --noconsole --onefile main.py
   ```

После сборки исполняемый файл будет находиться здесь:

`dist/main.exe`

Собрать можно и под **macOS** и под **Linux** - если собираешь на этих системах.

---

## 🧐 Что скрипт умеет

- **Видит всё:** Обнаруживает любые носители - **USB, SD, HDD, SSD, CD/DVD**, а также внезапно смонтированные локальные диски.
- **Гибкие режимы копирования:**
  - Ничего не копировать
  - Копировать только структуру каталогов
  - Копировать всё со структурой
  - Скопировать все файлы в одну плоскую папку
- **Генерация структуры:** Создаёт **JSON-структуру** каталогов для анализа.
- **Обработка дубликатов:** При совпадении имён файлов добавляет **микросекундный timestamp**, чтобы без `фото (1).jpg`.
- **Фильтрация:** Можно ограничить расширения файлов и максимальный размер.
- **Стабильность:** Работает даже на **Windows 7**.
- **Чистота:** Игнорирует системные каталоги и служебный мусор.
- **Надёжность:** Нормально переживает внезапное извлечение носителя.

---

## 🤷 Если тупой и лень разбираться

- Из каталога "Готовые exe", или из релизов, скачиваешь готовые exe файлы, запускаешь, пляшешь.

- Exeшник появится в процессах диспетчера задач. Никакое окно не запустится. Завершать скрипт тоже через диспетчер.

- При желании можно закинуть в **автозапуск Windows**.
