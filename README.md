# 😈 SpyCopy_USB-DVD

> О, флешка! Дай-ка я всё твоё добро спи... копирую.

**Плохая программа для скрытого сканирования и копирования внешних носителей.**

---

## 🛠 Возможности и совместимость

- **Кроссплатформенность:** Работает на **Windows, Linux, macOS** — хоть на утюге, если Python прикрутишь.
- **Сборка для Windows:** Для Windows можно собрать `.exe` и забыть, что Python вообще существует.
- **Автоопределение носителя:** Скрипт сам определяет, что вы воткнули — **флешку, SD-шку, внешний диск или даже CD/DVD** (да, они ещё бывают).

---

## 🚨 Как это работает

Как только носитель появляется — скрипт такой:

> «Поздравляю, родной. Сейчас мы посмотрим, что у тебя там внутри»

И создаёт аккуратную папку в `/catalogs` с датой, временем, таймстампом и именем носителя.  
Чтобы не перепутать все те 69 флешек, которые ты когда-то нашёл в шкафу.

---

## 🏗 Сборка в `.exe`

Используйте **PyInstaller** для создания автономного исполняемого файла.

1. **Установить PyInstaller**
   ```bash
   pip install pyinstaller
   ```

2. **Собрать проект**
   ```bash
   pyinstaller --noconsole --onefile main.py
   ```

После сборки исполняемый файл будет находиться здесь:

`dist/main.exe`

Собрать можно и под **macOS** и под **Linux** — если собираете на этих системах.

---

## 🧐 Что скрипт умеет

Вообще он умный. **Реально.**

- **Видит всё:** Обнаруживает любые носители — **USB, SD, HDD, SSD, CD/DVD**, а также внезапно смонтированные локальные диски.
- **Гибкие режимы копирования:**
  - Ничего не копировать
  - Копировать только структуру каталогов
  - Копировать всё со структурой
  - Скопировать все файлы в одну плоскую папку (устроить бардак)
- **Генерация структуры:** Создаёт **JSON-структуру** каталогов для анализа.
- **Обработка дубликатов:** При совпадении имён файлов добавляет **микросекундный timestamp**, без `фото (1).jpg`.
- **Фильтрация:** Можно ограничить расширения файлов и максимальный размер.
- **Стабильность:** Работает даже на **Windows 7**, которой давно пора на покой.
- **Чистота:** Игнорирует системные каталоги и служебный мусор.
- **Надёжность:** Нормально переживает внезапное извлечение носителя.

---

## 🤷 Если тупой и лень разбираться

Берёшь готовый `.exe` из папки **«Готовые exe»** или из релизов, запускаешь — и всё.

- Окно не появляется
- Процесс видно в диспетчере задач
- Завершение — тоже через диспетчер

При желании можно закинуть в **автозапуск Windows**.
